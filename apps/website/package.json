{
  "name": "vibesters-website",
  "version": "0.1.0",
  "private": true,
  "description": "Public documentation website for vibeste.rs — static, deployed to GitHub Pages.",
  "license": "MIT",
  "scripts": {
    "dev": "live-server --port=4321",
    "preview": "live-server --port=4321 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
